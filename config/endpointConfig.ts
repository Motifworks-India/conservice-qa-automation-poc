export const endpoints = {
    api: {
      users: {
        create: '/api/users',
        details: '/api/users/{id}',
        update: '/api/users/{id}',
        delete: '/api/users/{id}'
      }
    }
  };